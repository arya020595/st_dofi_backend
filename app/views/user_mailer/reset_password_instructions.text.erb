Hello <%= @user.name %>,

Someone requested a password reset for your DoFi account. If this was you, open the link below to choose a new password:

<%= @reset_password_url %>

This link will expire in <%= (Devise.reset_password_within / 1.hour).to_i %> hours. If you didn't request this, you can safely ignore this email.
