StrongMigrations.start_after = 0

# Automatically set a 10-second lock timeout before every migration so the
# migration fails fast rather than blocking the table indefinitely.
StrongMigrations.lock_timeout = 10.seconds
